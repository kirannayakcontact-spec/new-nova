# WhatsApp Bot

Run the structured entrypoint with:

```bash
node bot/index.js
```

The root `Gateway.js` remains authoritative so the existing Baileys authentication directory is not moved or invalidated.
